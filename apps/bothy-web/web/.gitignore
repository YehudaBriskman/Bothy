node_modules
dist
*.local
.vite
*.tsbuildinfo

# TypeScript incremental build cache - regenerated by `tsc -b` on every build, so
# tracking it makes the working tree dirty after any compile and produces noisy
# diffs that carry no information.
*.tsbuildinfo
