# AUTO-GENERATED by Tals manifests/scripts/preflight-ports.sh — do not hand-edit.
# Traefik file-provider routes for the Tals dev stack (host processes on this
# box). Ports track scripts/preflight-ports.sh. 502 when `tilt up` isn't
# running is expected. No SSO middleware: Tals ships its own auth.
http:
  routers:
    tals:      { rule: "Host(`tals.dev.test`)",      entryPoints: [web], service: tals }
    tals-api:  { rule: "Host(`api.tals.dev.test`)",  entryPoints: [web], service: tals-api }
    tals-auth: { rule: "Host(`auth.tals.dev.test`)", entryPoints: [web], service: tals-auth }
    tals-algo: { rule: "Host(`algo.tals.dev.test`)", entryPoints: [web], service: tals-algo }
  services:
    tals:      { loadBalancer: { servers: [ { url: "http://host.docker.internal:5173" } ] } }
    tals-api:  { loadBalancer: { servers: [ { url: "http://host.docker.internal:3003" } ] } }
    tals-auth: { loadBalancer: { servers: [ { url: "http://host.docker.internal:3002" } ] } }
    tals-algo: { loadBalancer: { servers: [ { url: "http://host.docker.internal:8000" } ] } }
